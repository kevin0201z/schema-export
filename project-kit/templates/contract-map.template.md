# 字段映射文档

> AI 快速摘要
>
> - 适用场景：前端、API、后端、数据库之间存在字段传递时读取。
> - 必须产出：业务含义与各层字段名的对应关系，以及例外说明。
> - 硬性阻断：同一业务含义存在多套字段名且原因不明时，不要直接放行。
> - 相关规则：`project-kit/ai-guide/05-contract-sync.md`
> - 可跳过场景：当前任务没有跨层字段传递。

用于约束前端字段、API 字段、后端字段、数据库字段的一致性。

| 业务含义 | 前端字段 | API 字段 | 后端字段 | 数据库字段 | 说明 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

## 命名规则

- 前端字段：
- API 字段：
- 后端字段：
- 数据库字段：

## 例外说明

如同一业务含义在不同层必须使用不同字段名，必须在此说明原因。

# Third-Party Code

本目录用于存放仓库内随源码分发的第三方依赖。

当前包含：

- `dm-go-driver/`：达梦数据库 Go 驱动源码副本，通过 `go.mod` 中的 `replace dm => ./third_party/dm-go-driver/dm` 接入。

维护约定：

- 这里的代码不按本项目自研包对待，升级时优先整体替换，不在业务改动中随意混改。
- 如果后续能切回外部模块依赖，应优先去掉本地副本，减轻仓库维护负担。

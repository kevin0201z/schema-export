name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch: {}

env:
  GOLANGCI_LINT_VERSION: v1.64.8
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  fmt:
    name: Check formatting
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python for pre-commit
      run: |
        python3 -m pip install --upgrade pip
        pip3 install --user pre-commit
      shell: bash
    - name: Set up Go for pre-commit
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'
    - name: Install golangci-lint for pre-commit
      env:
        GOLANGCI_LINT_VERSION: v1.64.8
      run: |
        export PATH="$(go env GOPATH)/bin:$HOME/.local/bin:$PATH"
        go install github.com/golangci/golangci-lint/cmd/golangci-lint@${GOLANGCI_LINT_VERSION}
    - name: Run pre-commit
      run: |
        export PATH="$(go env GOPATH)/bin:$HOME/.local/bin:$PATH"
        pre-commit run --all-files
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.26'
    - name: Check formatting
      run: |
        out=$(gofmt -s -l .)
        if [ -n "$out" ]; then
          echo "gofmt found files that need formatting:";
          echo "$out";
          exit 1;
        fi

  vet:
    name: go vet
    runs-on: ubuntu-latest
    needs: fmt
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.26'
    - name: Run go vet
      run: go vet ./...

  lint:
    name: golangci-lint
    runs-on: ubuntu-latest
    needs: vet
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.26'
    - name: Restore golangci-lint cache
      uses: actions/cache@v4
      with:
        path: |
          ${{ runner.temp }}/golangci-cache
          ${{ github.workspace }}/pkg/mod
        key: ${{ runner.os }}-golangci-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-golangci-
    - name: Install golangci-lint
      env:
        GOLANGCI_LINT_VERSION: v1.64.8
      run: |
        go install github.com/golangci/golangci-lint/cmd/golangci-lint@${GOLANGCI_LINT_VERSION}
    - name: Run golangci-lint
      env:
        PATH: ${{ env.PATH }}:$(go env GOPATH)/bin
      run: |
        $(go env GOPATH)/bin/golangci-lint run --timeout 5m

  test:
    name: Unit tests (matrix)
    runs-on: ubuntu-latest
    needs: lint
    strategy:
      matrix:
        go-version: [1.25, 1.26]
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: ${{ matrix.go-version }}
    - name: Cache Go modules
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/go-build
          ${{ github.workspace }}/pkg/mod
        key: ${{ runner.os }}-go-${{ matrix.go-version }}-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-${{ matrix.go-version }}-
    - name: Download dependencies
      run: |
        # prefer the Go proxy, fallback to direct
        go env -w GOPROXY=https://proxy.golang.org,direct
        go mod download
    - name: Run tests (full)
      run: |
        # run all tests verbosely and disable test caching to ensure sqlmock expectations run fresh
        go test ./... -v -count=1

  build:
    name: Build artifacts
    runs-on: ubuntu-latest
    needs: test
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.26'
    - name: Build
      run: make build || true
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: binaries
        path: build/
